#include <stdio.h>
int main(){
	int n,a,b,c;
	printf("N ===> ");
	scanf("%d",n);
	a = n /10;
	a = b %10;
	c = (b*10+a)*2;
	if(c>100) c=c-100;
	if(h<=50)printf("GOOD");		
	else 
	printf("OH MY GOD");
}